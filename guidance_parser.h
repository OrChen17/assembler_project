#ifndef GUIDANCE_PARSER
#define GUIDANCE_PARSER
#include "helper.h"

int parse_guidance(GuidingInstruction *guidance);
/* Gets a struct containing, parses it into a GuidingCell and puts in the data section */
#endif